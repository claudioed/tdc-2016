spring:
  profiles: default
  application:
    name: discovery
  cloud:
    config:
      uri: http://localhost:8001/
---
spring:
  profiles: docker
  application:
    name: discovery
  cloud:
    config:
      uri: http://configuration:8001/